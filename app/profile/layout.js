export const metadata = {
    title: "My Profile",
    description: "Manage your Edchatflow profile, privacy settings, and security.",
};

export default function ProfileLayout({ children }) {
    return children;
}
