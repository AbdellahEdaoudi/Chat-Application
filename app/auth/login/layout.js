export const metadata = {
    title: "Login",
    description: "Securely log into your Edchatflow account to continue your conversations.",
};

export default function LoginLayout({ children }) {
    return children;
}
