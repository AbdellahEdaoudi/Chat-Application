export const metadata = {
    title: "Register",
    description: "Create a new Edchatflow account and join the secure, seamless messaging platform.",
};

export default function RegisterLayout({ children }) {
    return children;
}
